import java.util.*;
import java.lang.*;

class prac{

    public static void main(String args[])
    {
        int quant, dist;

        Scanner sc = new Scanner(System.in);

        quant = sc.nextInt();
        dist = sc.nextInt();

        if((quant==0 && quant < 0) || (dist == 0 && dist < 0))
        {
            System.out.print("Invalid Input");
        }

    
    }
}